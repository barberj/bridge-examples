from setuptools import setup


setup(
    name='bridge-examples',
    version='0.0.1',
    author='Justin',
    author_email='barber.justin(at)gmail.com',
    install_requires=[
        "tornado >= 2.2",
    ]
)
